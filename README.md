# lerna and npm publish test

Commands:
```bash
npm init # to create package.json
npm login # or npm adduser
npm publish --access public
npx lerna publish # will publish both packages on npm and git
```